---
/** Phase 2 fills this in. Props are the contract; the body is a labeled placeholder. */
export interface Props {
  src: string; alt: string; rotate?: number; tape?: string;
}

---

<div class="stub">PhotoFrame placeholder</div>

<style>
  .stub {
    border: var(--border);
    padding: var(--gutter);
    font-weight: 700;
  }
</style>
