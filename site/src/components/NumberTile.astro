---
/** Phase 2 fills this in. Props are the contract; the body is a labeled placeholder. */
export interface Props {
  figure: string; label: string; offset?: boolean;
}

---

<div class="stub">NumberTile placeholder</div>

<style>
  .stub {
    border: var(--border);
    padding: var(--gutter);
    font-weight: 700;
  }
</style>
