---
/** Phase 2 fills this in. Props are the contract; the body is a labeled placeholder. */
export interface Props {
  name: 'worker' | 'student' | 'founder' | 'company' | 'profile' | 'knowledge' | 'community' | 'toolkit';
}
---

<div class="stub">Icon placeholder</div>

<style>
  .stub {
    border: var(--border);
    padding: var(--gutter);
    font-weight: 700;
  }
</style>
