---
/** Phase 2 fills this in. Props are the contract; the body is a labeled placeholder. */
export interface Props {
  title: string; body: string;
}

---

<div class="stub">StrikeCard placeholder</div>

<style>
  .stub {
    border: var(--border);
    padding: var(--gutter);
    font-weight: 700;
  }
</style>
