---
/** Phase 2 fills this in. Props are the contract; the body is a labeled placeholder. */
export interface Props {
  labels: string[];
}

---

<div class="stub">Flowchart placeholder</div>

<style>
  .stub {
    border: var(--border);
    padding: var(--gutter);
    font-weight: 700;
  }
</style>
